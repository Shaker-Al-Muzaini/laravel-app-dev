apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306        # Service port
      targetPort: 3306  # Container port
  selector:
    app: mysql
